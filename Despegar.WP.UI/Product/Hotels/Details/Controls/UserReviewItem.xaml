<UserControl
    x:Class="Despegar.WP.UI.Product.Hotels.Details.Controls.UserReviewItem"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Despegar.WP.UI.Product.Hotels.Details.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:conv="using:Despegar.WP.UI.Common.Converter"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <UserControl.Resources>
        <conv:StringToN1Converter x:Name="StringToN1Converter" />
        <conv:RatingToColorConverter x:Name="RatingToColorConverter"/>
        <conv:NullToVisibilityConverter x:Name="NullToVisibilityConverter"/>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        
        <!--Header Grid-->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="{StaticResource Block1p}"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0" Height="{StaticResource dBlock1p}"  Background="{Binding CustomReviews.rating , Converter={StaticResource RatingToColorConverter}}" VerticalAlignment="Top" />

            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="{Binding CustomReviews.name}"/>
                    
                <TextBlock Grid.Row="1">
                    <Run Text="{Binding CustomReviews.rating}"/>
                    <Run Text="- DesdeX"/>
                    <Run Text="{Binding CustomReviews.country}"/>
                </TextBlock>
            </Grid>
        </Grid>
        
        <!--Text Grid-->

        <Grid Grid.Row="1">
            <StackPanel>
                <TextBlock Text="{Binding CustomReviewsItem.description}" Visibility="{Binding  CustomReviewsItem.description , Converter={StaticResource NullToVisibilityConverter}}"/>

                <StackPanel Visibility="{Binding  CustomReviewsItem.description , Converter={StaticResource NullToVisibilityConverter}}">
                    <TextBlock Text="Lo BuenoX"/>
                    <TextBlock Text="{Binding CustomReviewsItem.good}" />
                </StackPanel>

                <StackPanel Visibility="{Binding  CustomReviewsItem.description , Converter={StaticResource NullToVisibilityConverter}}">
                    <TextBlock Text="MaloX"/>
                    <TextBlock Text="{Binding CustomReviewsItem.bad}" />
                </StackPanel>
            </StackPanel>
        </Grid>
        
    </Grid>
</UserControl>
