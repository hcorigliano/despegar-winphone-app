<UserControl
    x:Class="Despegar.WP.UI.Product.Hotels.Details.Controls.BedSelectionControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Despegar.WP.UI.Product.Hotels.Details.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="{StaticResource Block2}"/>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <Grid Width="{StaticResource dBlock2}" Height="{StaticResource dBlock2}" Grid.Column="0" Background="{StaticResource Blue40Brush}" VerticalAlignment="Top">
            <Image Source="/Assets/Icon/App/bed-medium-white.scale-100.png"/>
        </Grid>

        <ComboBox Grid.Column="1" ItemsSource="{Binding rooms[0].bed_options}" SelectedItem="{Binding rooms[0].bed_options[0],Mode=TwoWay}" DisplayMemberPath="description"  VerticalAlignment="Center" Margin="{StaticResource Left_Offset}" Style="{StaticResource ComboBoxBase}" SelectionChanged="BedSelecionChanged" />

    </Grid>
</UserControl>
