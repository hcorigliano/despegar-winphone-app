<Page xmlns:Flights="using:Despegar.WP.UI.Controls.Flights" 
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Despegar.WP.UI.Product.Flights"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:UI="using:Despegar.WP.UI"
    xmlns:Model="using:Despegar.WP.UI.Model"
    x:Class="Despegar.WP.UI.Product.Flights.FlightResults"
    mc:Ignorable="d"
    Background="{ThemeResource AppBarBorderThemeBrush}" 
    
    >
    <Page.Resources>
        <DataTemplate x:Key="FlightItemDateTemplate">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="50"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="50"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="100"/>
                </Grid.ColumnDefinitions>

                <Image Grid.Column="0" HorizontalAlignment="Left" Height="Auto"  VerticalAlignment="Top" Width="Auto"/>
                <StackPanel Grid.Column="1"  HorizontalAlignment="Left" Height="Auto"  VerticalAlignment="Top" Width="Auto">
                    <TextBlock TextWrapping="Wrap" Text="{Binding airline.name}" />
                    <TextBlock TextWrapping="Wrap" Text="{Binding outbound.Count}"/>
                </StackPanel>
                <TextBlock Grid.Column="2"  HorizontalAlignment="Left" TextWrapping="Wrap" Text="{Binding price}" VerticalAlignment="Top"/>
                <ItemsControl x:Name="RoutesItemControl"  Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3" ItemsSource="{Binding Routes}" DataContext="{Binding}" Visibility="Collapsed">
                	
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Tapped="Grid_Tapped">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="20"/>
                                    <RowDefinition Height="20"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="25"/>
                                    <ColumnDefinition Width="30"/>
                                    <ColumnDefinition Width="30"/>
                                    <ColumnDefinition Width="30"/>
                                    <ColumnDefinition Width="30"/>
                                    <ColumnDefinition Width="30"/>
                                    <ColumnDefinition Width="50"/>
                                    <ColumnDefinition Width="50"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition />
                                </Grid.ColumnDefinitions>
                                <Image Grid.Column="0" HorizontalAlignment="Left" Height="50" Margin="10,60,0,0" Grid.Row="0" VerticalAlignment="Top" />
                                <TextBlock Grid.Column="1" HorizontalAlignment="Left" TextWrapping="Wrap" Text="{Binding outbound.from.code}" VerticalAlignment="Top"/>
                                <TextBlock Grid.Column="2" HorizontalAlignment="Left" Margin="0,0,0,0" TextWrapping="Wrap" Text="{Binding outbound.departure}" VerticalAlignment="Top"/>
                                <Image Grid.Column="3" HorizontalAlignment="Left" Height="50" Margin="0,0,0,0" VerticalAlignment="Top" />
                                <TextBlock Grid.Column="4" HorizontalAlignment="Left" Margin="0,0,0,0" TextWrapping="Wrap" Text="{Binding outbound.to.code}" VerticalAlignment="Top"/>
                                <TextBlock Grid.Column="5" HorizontalAlignment="Left" Margin="0,0,0,0" TextWrapping="Wrap" Text="{Binding outbound.arrival}" VerticalAlignment="Top"/>
                                <TextBlock Grid.Column="6" HorizontalAlignment="Left" Margin="0,0,0,0" TextWrapping="Wrap" Text="{Binding outbound.duration}" VerticalAlignment="Top"/>
                                <TextBlock Grid.Column="7" HorizontalAlignment="Left" Margin="0,0,0,0" TextWrapping="Wrap" Text="{Binding outbound.layovers}" VerticalAlignment="Top"/>

                                <Image Grid.Column="0" HorizontalAlignment="Left" Height="50" Margin="10,60,0,0" VerticalAlignment="Top" Grid.Row="1" />
                                <TextBlock Grid.Column="1" HorizontalAlignment="Left" Margin="0,0,0,0" TextWrapping="Wrap" Text="{Binding inbound.from.code}" VerticalAlignment="Top" Grid.Row="1"/>
                                <TextBlock Grid.Column="2" HorizontalAlignment="Left" Margin="0,0,0,0" TextWrapping="Wrap" Text="{Binding inbound.departure}" VerticalAlignment="Top" Grid.Row="1"/>
                                <Image Grid.Column="3" HorizontalAlignment="Left" Height="50" Margin="0,0,0,0" VerticalAlignment="Top"  Grid.Row="1"/>
                                <TextBlock Grid.Column="4" HorizontalAlignment="Left" Margin="0,0,0,0" TextWrapping="Wrap" Text="{Binding inbound.to.code}" VerticalAlignment="Top" Grid.Row="1"/>
                                <TextBlock Grid.Column="5" HorizontalAlignment="Left" Margin="0,0,0,0" TextWrapping="Wrap" Text="{Binding inbound.arrival}" VerticalAlignment="Top" Grid.Row="1"/>
                                <TextBlock Grid.Column="6" HorizontalAlignment="Left" Margin="0,0,0,0" TextWrapping="Wrap" Text="{Binding inbound.duration}" VerticalAlignment="Top" Grid.Row="1"/>
                                <TextBlock Grid.Column="7" HorizontalAlignment="Left" Margin="0,0,0,0" TextWrapping="Wrap" Text="{Binding inbound.layovers}" VerticalAlignment="Top" Grid.Row="1"/>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>

        </DataTemplate>
    </Page.Resources>

    <Page.DataContext>
        <Model:FlightResultsModel/>
    </Page.DataContext>

    <Grid x:Name="LayoutRoot">

        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="19,0,0,0">
            <TextBlock x:Uid="AppTitle" Style="{ThemeResource TitleTextBlockStyle}" Margin="0,12,0,0"/>
            <TextBlock x:Uid="Page_FlightResults" Margin="0,-6.5,0,26.5" Style="{ThemeResource HeaderTextBlockStyle}" CharacterSpacing="{ThemeResource PivotHeaderItemCharacterSpacing}"/>

            <UI:SearchMiniBoxControl Height="Auto"/>
        </StackPanel>

        <Grid Grid.Row="1">
            <TextBlock />
        </Grid>
        <Grid Grid.Row="2" x:Name="ContentRoot" Width="Auto" >
            <ListBox Name="lbFlights" Background="White" ItemTemplate="{StaticResource FlightItemDateTemplate}" ItemsSource="{Binding Items}"  Tapped="ListBox_Tapped"  />
        </Grid>

    </Grid>
</Page>
