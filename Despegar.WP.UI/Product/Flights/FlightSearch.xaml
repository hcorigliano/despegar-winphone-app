<Page
    x:Class="Despegar.WP.UI.Product.Flights.FlightSearch"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Despegar.WP.UI.Product.Flights"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid x:Name="LayoutRoot">

        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title Panel -->
        <StackPanel Grid.Row="0" Margin="19,0,0,0">
            <TextBlock x:Uid="AppTitle" Style="{ThemeResource TitleTextBlockStyle}" Margin="0,12,0,0"/>
            <TextBlock x:Uid="Page_FlightSearch" Margin="0,-6.5,0,26.5" Style="{ThemeResource HeaderTextBlockStyle}" CharacterSpacing="{ThemeResource PivotHeaderItemCharacterSpacing}"/>
        </StackPanel>

        <!--TODO: Content should be placed within the following grid-->
        <Grid Grid.Row="1" x:Name="ContentRoot" Margin="19,9.5,19,0">
            <StackPanel  VerticalAlignment="Top" HorizontalAlignment="Stretch">
                <TextBlock>Origen</TextBlock>
                <TextBox Text="Miami, Florida, Estados Unidos"></TextBox>
                <TextBlock>Destino</TextBlock>
                <TextBox Text="Buenos Aires, Argentina"></TextBox>
                <StackPanel
                    Orientation="Horizontal"
                    >
                    <StackPanel> 
                        <TextBlock>Partida</TextBlock>
                        <DatePicker></DatePicker>
                    </StackPanel>
                    <StackPanel>
                        <TextBlock>Regreso</TextBlock>
                        <DatePicker></DatePicker>
                    </StackPanel>
                </StackPanel>
                
                <TextBlock Text="Adultos (+12)     - 1 +"></TextBlock>
                <TextBlock Text="0 - 11     - 0 +" ></TextBlock>
            </StackPanel>
            
            <Button Click="Button_Click" Margin="109,443,0,1">Buscar vuelos</Button>
        </Grid>
    </Grid>
</Page>
