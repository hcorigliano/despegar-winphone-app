<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Despegar.WP.UI.Product.Flights"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Interactivity="using:Microsoft.Xaml.Interactivity" xmlns:Core="using:Microsoft.Xaml.Interactions.Core"
    x:Class="Despegar.WP.UI.Product.Flights.FlightDetail"
    xmlns:src="using:Despegar.WP.UI.Controls.Flights"
    xmlns:dev="using:Despegar.WP.UI.Developer.Controls"
    mc:Ignorable="d"
    Style="{StaticResource BasePage}">

    <Grid x:Name="LayoutRoot" Style="{StaticResource MainContainerGrid}">
		<Grid.ChildrenTransitions>
			<TransitionCollection>
				<EntranceThemeTransition/>
			</TransitionCollection>
		</Grid.ChildrenTransitions>
		<Grid.RowDefinitions>
            <RowDefinition Height="{StaticResource Block1p}"/>
            <RowDefinition Height="{StaticResource Block1p}"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>
        
		<!-- Title Panel -->
        <Image Grid.Row="0" Style="{StaticResource TopLogo}" />
        <TextBlock Grid.Row="1" x:Uid="Page_Detail_TitleDetail" Style="{StaticResource PageHeaderSmall}" Text="DETALLE" />

        <!-- Content -->
        <Pivot Grid.Row="2" Style="{StaticResource PivotBaseMain}" x:Name="MainPivot">
            
            <PivotItem x:Uid="Page_Flight_Detail_Go" Header="ida" Style="{StaticResource PivotItemBase}" DataContext="{Binding OutModel}">
                <ScrollViewer>
                    <Grid>                                                
                        <Grid.RowDefinitions>
                            <RowDefinition Height="{StaticResource Block2p}"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- DEV -->
                        <!--<dev:UserControlDesignGrid />-->
                        
                        <!-- Route Header -->
                        <Grid Grid.Row="0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="{StaticResource Block2p}" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <Grid Grid.Column="0" Style="{StaticResource IconGreenBig}">
                                <Image Style="{StaticResource FlightDetailAirlineImage}" />
                            </Grid>

                            <Grid Grid.Column="1">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="{StaticResource Block1p}" />
                                    <RowDefinition Height="{StaticResource Square}" />
                                </Grid.RowDefinitions>

                                <TextBlock Grid.Row="0" Text="{Binding Path=from.city}" Style="{StaticResource FlightDetailTitleGreen}" />
                                <TextBlock Grid.Row="1" Text="{Binding Path=to.city}" Style="{StaticResource FlightSegmentSmallText}"  />
                            </Grid>
                        </Grid>

                        <!-- Outbound Segments -->
                        <ItemsControl Grid.Row="1"  x:Name="SegmentControlOutbound" ItemsSource="{Binding segments}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <src:FlightSegment IsReturn="false" />
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Grid>
                </ScrollViewer>
            </PivotItem>

            <PivotItem x:Name="ReturnPivot" x:Uid="Page_Flight_Detail_Return" Header="vuelta" Style="{StaticResource PivotItemBase}" DataContext="{Binding InModel}">
                <ScrollViewer>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="{StaticResource Block2p}"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- Route Header -->
                        <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="{StaticResource Block2p}"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                            <Grid Grid.Column="0" Style="{StaticResource IconLightBlueBig}">
                                <Image Style="{StaticResource FlightDetailAirlineReturnImage}" />
                            </Grid>
                            
                            <Grid Grid.Column="1">
                             <Grid.RowDefinitions>
                                <RowDefinition Height="{StaticResource Block1p}" />
                                <RowDefinition Height="{StaticResource Square}" />
                             </Grid.RowDefinitions>

                                <TextBlock Grid.Row="0" Text="{Binding from.city}" Style="{StaticResource FlightReturnSegmentTitle}" />
                                <TextBlock Grid.Row="1" Text="{Binding to.city}" Style="{StaticResource FlightSegmentSmallText}"  />
                        </Grid>
                    </Grid>
                        
                       <!-- Inbound Segments -->
                        <ItemsControl Grid.Row="1" x:Name="SegmentControlInbound" ItemsSource="{Binding segments}">
						<ItemsControl.ItemTemplate>
							<DataTemplate>
                                  <src:FlightSegment IsReturn="true" />
							</DataTemplate>
						</ItemsControl.ItemTemplate>
					</ItemsControl>
                    
                    </Grid>
                </ScrollViewer>
            </PivotItem>
            
        </Pivot>
        
        <!-- Footer -->
        <Button Grid.Row="3" x:Uid="Page_Flight_Detail_Buy.Text" Content="Comprar" Style="{StaticResource ButtonBottomRed}" Command="{Binding BuyCommand}"/>       
	</Grid>
</Page>