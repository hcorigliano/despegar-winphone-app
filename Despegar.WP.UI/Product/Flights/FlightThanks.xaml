<Page
    x:Class="Despegar.WP.UI.Product.Flights.FlightThanks"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Despegar.WP.UI.Product.Flights"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:src="using:Despegar.WP.UI.Controls.Flights.Detail"
    xmlns:srcc="using:Despegar.WP.UI.Product.Flights.Checkout.Controls"
    xmlns:dev="using:Despegar.WP.UI.Developer.Controls"
    xmlns:conv="using:Despegar.WP.UI.Common.Converter"
    xmlns:cont="using:Despegar.WP.UI.Controls"
    mc:Ignorable="d"
    Style="{StaticResource BasePage}">

    <Page.Resources>
        <conv:VisibilityConverter x:Key="visibilityConverter" TrueValue="Visible" FalseValue="Collapsed"  />
        <conv:VisibilityConverter x:Key="invertedConverter" TrueValue="Collapsed" FalseValue="Visible"  />
    </Page.Resources>
    
    <Grid x:Name="LayoutRoot" Style="{StaticResource MainContainerGrid}" >

        <!-- Dev -->
        <!--<dev:UserControlDesignGrid  />-->

        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="{StaticResource Block1p}"/>
            <RowDefinition Height="{StaticResource Block1p}"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Title Panel -->
        <cont:TopLogo Grid.Row="0"  />
        <TextBlock Grid.Row="1" x:Uid="Page_FlightSearch" Style="{StaticResource PageHeaderSmall}" Text="BUSQUEDA"  />


        <ScrollViewer Grid.Row="2">
            <StackPanel>
                <TextBlock Grid.Row="1" x:Uid="Flight_Thanks" Style="{StaticResource Thanks_PageHeader}" Text="gracias totales" />
                <!--<TextBlock Text="¡Muchas gracias por su solicitud de compra!" />-->
                <TextBlock  Style="{StaticResource Thanks_Request_Is }" x:Uid="Flight_Thanks_Request_Is" Text="Su número de solicitud de compra es:"  />
                <TextBlock Style="{StaticResource Thanks_Request }" Text="{Binding flightCrossParameters.BookingResponse.checkout_id}"/>
                <TextBlock Style="{StaticResource Thanks_How_Continue}" x:Uid="Flight_Thanks_How_Continue" Text="¿Cómo continúa el proceso de compra?" />

                <!-- Emitted or charged? -->
                <TextBlock TextWrapping="Wrap" Style="{StaticResource Thanks_Continue_Explain}" 
                           x:Uid="Flight_Thanks_Continue_Explain" 
                           Text="Su pago está siendo procesado. Una vez que el pago se procese exitosamente recibirá un e-mail con la información de compra."  
                           Visibility="{Binding flightCrossParameters.IsETicketed, Converter={StaticResource invertedConverter}}" />

                
                <TextBlock TextWrapping="Wrap" Style="{StaticResource Thanks_Continue_Explain}" 
                           x:Uid="Flight_Thanks_Continue_Explain_Emitted" 
                           Text="Ha sido emitido pero no cobrado"
                           Visibility="{Binding flightCrossParameters.IsETicketed, Converter={StaticResource visibilityConverter}}"
                           />
                <!-- / Emitted or charged? -->

                <TextBlock Style="{StaticResource Thanks_Booking_Detail_Blue}" x:Uid="Common_Booking_Detail" Text="Detalle de reserva" />
                <StackPanel xmlns:src="using:Despegar.WP.UI.Product.Flights.Checkout.Controls">
                    <src:BuyDetailsControl DataContext="{Binding flightCrossParameters.PriceDetail}" />
                </StackPanel>

                <!-- Visa and Vaccines -->
                <srcc:VisaAndVaccines Grid.Row="1" DataContext="{Binding }" Margin="0,8,0,0" />
                
                <!-- Flight Data -->
                <TextBlock Style="{StaticResource Thanks_Booking_Detail_Green}" x:Uid="Generic_Go" Text="IDA" />
                <src:FlightsSegmentGo DataContext="{Binding flightCrossParameters.Outbound}"  x:Name="FlightsSegmentGoSrc"/>

                <ItemsControl ItemsSource="{Binding flightCrossParameters.MultipleRoutes}" x:Name="FlightsSegmentGoSrcMultiple">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <src:FlightsSegmentGo DataContext="{Binding outbound}"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <TextBlock Style="{StaticResource Thanks_Booking_Detail_Blue}" x:Uid="Generic_Return" Text="VUELTA" x:Name="FlightsSegmentReturnTextBlock"/>
                <src:FlightsSegmentReturn DataContext="{Binding flightCrossParameters.Inbound}" x:Name="FlightsSegmentReturnSrc" />
                


            </StackPanel>
        </ScrollViewer>

        <Button Style="{StaticResource Thanks_Button}" Grid.Row="3" x:Uid="Generic_Acept" Content="Aceptar" HorizontalAlignment="Center" Command="{Binding NavigateToHomeCommand}" />

    </Grid>
</Page>
