<UserControl
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Despegar.WP.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Converter="using:Despegar.WP.UI.Common.Converter"
    xmlns:System="using:System"
    x:Class="Despegar.WP.UI.Controls.DateDisplayMiniBoxSearch"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400" Height="Auto" Width="Auto">


    <UserControl.Resources>
        <Converter:GetMonthName x:Key="GetMonthName"/>
        <Converter:DayofWeekShortName x:Key="DayofWeekShortName"/>
    </UserControl.Resources>


    <Grid Width="Auto" Height="Auto" >
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" HorizontalAlignment="Left" Height="Auto" VerticalAlignment="Top" Width="Auto">
            <TextBlock  TextWrapping="Wrap" Text="{Binding LocalDateTime.DayOfWeek, Converter={StaticResource DayofWeekShortName}}" />
            <TextBlock Text="{Binding LocalDateTime.Month, Converter={StaticResource GetMonthName}}"/>
        </StackPanel>
        <StackPanel Grid.Column="1">
            <TextBlock Text="{Binding LocalDateTime.Day, Mode=OneWay}"  />
        </StackPanel>
    </Grid>
</UserControl>
