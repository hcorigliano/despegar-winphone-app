<UserControl
    x:Class="Despegar.WP.UI.Controls.Field"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Despegar.WP.UI.Controls"
    xmlns:conv="using:Despegar.WP.UI.Common.Converter"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:dev="using:Despegar.WP.UI.Developer.Controls"
    mc:Ignorable="d">

    <StackPanel Style="{StaticResource CheckoutFieldContainer}">
        <TextBlock Style="{StaticResource BaseTextLabel}" Text="{Binding Label}" />
        <TextBox x:Name="InnerTextbox" Style="{StaticResource CheckoutInputBase}" Text="{Binding Value, Mode=TwoWay}"  PlaceholderText="{Binding PlaceholderText}" TextChanged="TextBox_TextChanged" />
    </StackPanel>

</UserControl>