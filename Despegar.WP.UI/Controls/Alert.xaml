<UserControl
    x:Class="Despegar.WP.UI.Controls.Alert"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Despegar.WP.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:dev="using:Despegar.WP.UI.Developer.Controls"
    d:DesignHeight="{StaticResource dBlock2}"
    d:DesignWidth="400">
    
    <Grid Style="{StaticResource AlertBlue}" Height="{StaticResource dBlock2}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="{StaticResource Block1p}" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <!--<dev:UserControlDesignGrid />-->
        <Grid Grid.Column="0" Style="{StaticResource IconWhiteSmall}" Margin="5,6,0,0">
            <TextBlock  Style="{StaticResource AlertGlyph}">
                &#xE171;
            </TextBlock>
        </Grid>
        
        <TextBlock Grid.Column="1" Style="{StaticResource AlertWhiteText}" Margin="0,3,0,0" Text="{Binding Text}" />

    </Grid>
</UserControl>