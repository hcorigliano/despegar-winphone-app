<UserControl
    x:Class="Despegar.WP.UI.Controls.Loading"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Despegar.WP.UI.Developer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    
    <UserControl.Resources>
        <Storyboard x:Name="loadingStoryboard" RepeatBehavior="Forever" />
    </UserControl.Resources>
    
    <Grid Width="{Binding Width}" Height="{Binding Height}" Opacity="1" x:Name="mainCanvas" >
        <Grid.RowDefinitions>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.Background>
            <SolidColorBrush Color="Black" Opacity="0.6"/>
        </Grid.Background>
        
        <!--- TODO: Move to Styles -->
        
            <Image x:Name="ImageView" 
                HorizontalAlignment="Center" 
                VerticalAlignment="Center"
                Stretch="Fill"
                   Width="120"
                   Height="120" />
            <!--<TextBlock Style="{StaticResource SegoeSemilight17}"  Text="Buscando vuelos..." VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,140,0,0" />-->
       
        
        <!--<Rectangle Width="100" Height="100" Fill="Red" />-->
    </Grid>

</UserControl>