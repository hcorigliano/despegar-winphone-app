<UserControl
    x:Class="Despegar.WP.UI.Controls.ValidationError"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Despegar.WP.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:conv="using:Despegar.WP.UI.Common.Converter"    
    mc:Ignorable="d"
    MinHeight="{StaticResource dBlock1}"
    >
    
    <UserControl.Resources>
        <conv:NullToOpacityConverter x:Name="OpacityConv" />
    </UserControl.Resources>

    <Grid Margin="0,7,0,0" Opacity="{Binding ErrorCode, Converter={StaticResource OpacityConv}}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="{StaticResource Square}" />
            <ColumnDefinition Width="5" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Style="{StaticResource IconRedSmall}">
            <TextBlock  Style="{StaticResource CheckkoutValidationGlyph}">
                &#xE171;
            </TextBlock>
        </Grid>
        <TextBlock Grid.Column="2"  x:Name="TextLabel" Style="{StaticResource CheckoutValidationErrorText}" Text="Error e argentina sos un wachin" />
    </Grid>
</UserControl>