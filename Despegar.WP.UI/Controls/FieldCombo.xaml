<UserControl
    x:Class="Despegar.WP.UI.Controls.FieldCombo"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Despegar.WP.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:dev="using:Despegar.WP.UI.Developer.Controls"
    mc:Ignorable="d"
    >

    <StackPanel Style="{StaticResource CheckoutFieldContainer}">
        <!--<dev:UserControlDesignGrid />-->
        <TextBlock Style="{StaticResource BaseTextLabel}" Text="{Binding Label}"  />
        <ComboBox x:Name="fieldCombo"
                  ItemsSource="{Binding ItemsSource}" 
                  SelectedValue="{Binding Value, Mode=TwoWay}"
                  SelectedValuePath="{Binding CodePath}" 
                  DisplayMemberPath="{Binding DisplayTextPath}"                   
                  Style="{StaticResource ComboBoxBase}" />
    </StackPanel>

</UserControl>