<UserControl
    x:Class="Despegar.WP.UI.Controls.Flights.ChildControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Despegar.WP.UI.Controls.Flights"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <StackPanel>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"></RowDefinition>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"  />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition MinWidth="150" Width="auto"  />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Grid.Row="0" Text="Edad del menor "></TextBlock>
                <TextBlock Grid.Column="1" Grid.Row="0" x:Name="IdChild" Text="{Binding Quantity}"></TextBlock>
                <ComboBox  Grid.Column="2" Grid.Row="0"  x:Name="ComboBoxOptions" HorizontalAlignment="Right" ></ComboBox>
                
            </Grid>
            
        </StackPanel>
    </Grid>
</UserControl>
