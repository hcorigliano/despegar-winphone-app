<UserControl
    x:Class="Despegar.WP.UI.Controls.Flights.DateControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Despegar.WP.UI.Controls.Flights"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:dev="using:Despegar.WP.UI.Developer.Controls"
    mc:Ignorable="d"     
    >

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="{StaticResource TwoColumnMiddleSpacing}" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>        

        <StackPanel Grid.Column="0" >
            <TextBlock  Style="{StaticResource BaseTextLabel}" x:Uid="Flights_Search_Departure" Text="Partida" />
            <DatePicker Style="{StaticResource DatePickerBase}" x:Name="departure" DateChanged="departure_DateChanged"/>
        </StackPanel>

        <StackPanel Grid.Column="2">
            <TextBlock Style="{StaticResource BaseTextLabel}"   x:Uid="Flights_Search_Return"  Text="Regreso" />
            <DatePicker Style="{StaticResource DatePickerBase}" x:Name="returnn"/>
        </StackPanel>
    </Grid>

</UserControl>