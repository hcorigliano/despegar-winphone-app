<UserControl
    x:Class="Despegar.WP.UI.Controls.Flights.Results.PaxCounterMiniBoxSearch"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Despegar.WP.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
	Height="Auto"
	Width="Auto"
	>
    
    <Grid>
    	<Grid.ColumnDefinitions>
    		<ColumnDefinition Width="35"/>
    		<ColumnDefinition/>
    	</Grid.ColumnDefinitions>
        
        <Grid.Resources>
            
        </Grid.Resources>

        <Grid Grid.Column="0" Style="{StaticResource IconBlue}">
            <Image Style="{StaticResource FamilyIconResult}"/>
        </Grid>
        <StackPanel Grid.Column="1" HorizontalAlignment="Left" Height="Auto" VerticalAlignment="Top" Width="Auto">
            <TextBlock TextWrapping="Wrap" Text="{Binding TotalPassangers}" Style="{StaticResource SegoeLight24}"/>
            <TextBlock TextWrapping="Wrap" x:Uid="Page_FlightResult_Passangers" Style="{StaticResource SegoeRegular15}"/>
        </StackPanel>
    </Grid>

</UserControl>
