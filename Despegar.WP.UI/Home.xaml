<Page
    x:Class="Despegar.WP.UI.Home"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Despegar.WP.UI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:dev="using:Despegar.WP.UI.Developer.Controls"
    mc:Ignorable="d"  
    Style="{StaticResource BasePage}"
    NavigationCacheMode="Required"
    >
    <Page.BottomAppBar>
        <CommandBar ClosedDisplayMode="Minimal">
            <AppBarButton Icon="Setting" x:Uid="Home_Settings" FontSize="14" Command="{Binding NavigateToCountrySelection}" />
        </CommandBar>
    </Page.BottomAppBar>

    <Grid x:Name="LayoutRoot" Style="{StaticResource MainContainerGrid}">
        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="{StaticResource Block1p}"/>
            <RowDefinition Height="{StaticResource Block2p}"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- DEV -->
        <!--<dev:UserControlDesignGrid />-->

        <!-- Title Section -->
        <Image Grid.Row="0" Style="{StaticResource TopLogo}" />
        <TextBlock  Grid.Row="1" x:Uid="Page_Home" Style="{StaticResource PageHeader}" Text="inicio" Grid.RowSpan="2" />

        <ListView Grid.Row="2"  IsItemClickEnabled="True" ItemClick="ListView_ItemClick" ItemContainerStyle="{StaticResource HomeButtonItemContainer}" Margin="{StaticResource Top_Offset}">
            <ListViewItem x:Name="Hotels" Visibility="Collapsed" Margin="{StaticResource Top_Offset}">
                <Grid DataContext="Hotels">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="{StaticResource Block3}"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Grid Grid.Column="0" Style="{StaticResource BaseHomeButtonBackground}">
                        <Image Style="{StaticResource HomeHotelsButton}"  />
                    </Grid>
                    
                    <StackPanel Grid.Column="1"  Style="{StaticResource HomeButtonTextContainer}">
                        <TextBlock x:Uid="Page_Home_HotelsButton"     Style="{StaticResource HomeButtonTitle}" Text="Hoteles" />
                        <!--<TextBlock x:Uid="Page_Home_HotelsButtonSub"  Style="{StaticResource HomeButtonSub}" Text="Las mejoires ofertais" />-->
                    </StackPanel>
                </Grid>
            </ListViewItem>

            <ListViewItem x:Name="Flights" Visibility="Collapsed">
                <Grid DataContext="Flights">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="{StaticResource Block3}"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Grid Grid.Column="0" Style="{StaticResource BaseHomeButtonBackground}">
                        <Image Style="{StaticResource HomeFlightsButton}"  />
                    </Grid>
                    
                    <StackPanel Grid.Column="1"  Style="{StaticResource HomeButtonTextContainer}" >
                        <TextBlock x:Uid="Page_Home_FlightsButton"     Style="{StaticResource HomeButtonTitle}" Text="Vuelos" />
                        <!--<TextBlock x:Uid="Page_Home_FlightsButtonSub"  Style="{StaticResource HomeButtonSub}" Text="Las mejoires ofertais" />-->
                    </StackPanel>
                </Grid>
            </ListViewItem>

            <ListViewItem>
                <Grid DataContext="FlightStatus">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="{StaticResource Block3}"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Grid Grid.Column="0" Style="{StaticResource BaseHomeButtonBackground}">
                        <Image Style="{StaticResource HomeFlightStatusButton}"  />
                    </Grid>
                    
                    <StackPanel Grid.Column="1"  Style="{StaticResource HomeButtonTextContainer}">
                        <TextBlock x:Uid="Page_Home_FlightStatusButton"     Style="{StaticResource HomeButtonTitle}" Text="Estado de Vuelos" />
                        <!--<TextBlock x:Uid="Page_Home_FlightStatusButtonSub"  Style="{StaticResource HomeButtonSub}" Text="Las mejoires ofertais" />-->
                    </StackPanel>
                </Grid>
            </ListViewItem>

            <ListViewItem>
                <Grid DataContext="MyDespegar">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="{StaticResource Block3}"/>
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <Grid Grid.Column="0" Style="{StaticResource BaseHomeButtonBackground}">
                        <Image Style="{StaticResource HomeMyDespegarButton}"  />
                    </Grid>
                    
                    <StackPanel Grid.Column="1" Style="{StaticResource HomeButtonTextContainer}">
                        <TextBlock x:Uid="Page_Home_MyDespegarButton"     Style="{StaticResource HomeButtonTitle}" Text="Hoteles"  />
                        <!--<TextBlock x:Uid="Page_Home_MyDespegarButtonSub"  Style="{StaticResource HomeButtonSub}" Text="Las mejoires ofertais" />-->
                    </StackPanel>
                </Grid>
            </ListViewItem>
        </ListView>

    </Grid>
</Page>